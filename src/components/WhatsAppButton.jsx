// File intentionally left empty as part of the removal process.
// (System limitation: files cannot be deleted, so we export a null component)
const WhatsAppButton = () => null;
export default WhatsAppButton;